#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int testcase, n;
    cin >> testcase;
    while (testcase--)
    {
        int n;
        cin >> n;
        cout << int(sqrt(n)) << endl;
    }
    return 0;
}
